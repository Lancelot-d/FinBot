FROM python:3.10.6-slim-bullseye

COPY ./reddit_api /reddit_api

WORKDIR /reddit_api

RUN pip install --upgrade pip

RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 8080/tcp

CMD ["python", "-u", "-m", "uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8080"]